// Public dashboard — stub. Today/Week/Block/Lab land in a later phase
// (DASHBOARD_SPEC.md §12, Phase 1+). This bundle must never import
// @supabase/supabase-js or anything under src/log/ — see CLAUDE.md and
// DASHBOARD_SPEC.md §4 on bundle isolation.
export default function App() {
  return (
    <div className="page">
      <h1>Luca — Training Dashboard</h1>
      <p className="status-text status-text--muted">Coming soon.</p>
    </div>
  )
}
