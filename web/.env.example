# Copy to .env.local (gitignored) and fill in real values.
# The publishable key is public by design (RLS + table grants are the
# actual boundary) — see CLAUDE.md hard rule 8 and DASHBOARD_SPEC.md §11.5.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# GitHub Pages subpath, e.g. /luca-dashboard/. Defaults to "/" for local dev.
VITE_BASE_PATH=
